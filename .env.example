# ─── ICUNI Labs Website — Environment Variables ───
# Copy to `.env` and fill in. `.env` is gitignored — never commit it.

# ICUNI Ops unified backend (Apps Script Web App deployment URL).
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_OPS_DEPLOYMENT_ID/exec
